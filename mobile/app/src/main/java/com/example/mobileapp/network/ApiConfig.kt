package com.example.mobileapp.network

/**
 * EDIT THIS to point at your backend. Mirrors src/apiUrl.js from the React app.
 *
 * - Local backend, Android EMULATOR:  "http://10.0.2.2:8080/api"
 *     (10.0.2.2 is the emulator's alias for your computer's "localhost")
 * - Local backend, REAL DEVICE on the same Wi-Fi:  "http://192.168.x.x:8080/api"
 *     (use your computer's LAN IP, not "localhost")
 * - Deployed/live backend:  "https://your-deployed-api.com/api"
 *
 * Placeholder below - change it before building.
 */
object ApiConfig {
    const val BASE_URL="https://cargo-p1in.onrender.com/api"
}
